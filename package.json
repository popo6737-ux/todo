{
  "name": "sokcho-workshop-compare",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "mkdir -p dist && cp src/index.html src/style.css src/app.js src/model.js dist/",
    "test": "node --test tests/*.test.mjs"
  }
}
